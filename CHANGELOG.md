# Changelog

## 2026-03-07

- Documentation under `docs/` is intentionally ignored in this repository.
- Trust public docs are published via snapshot export (`pnpm public:export`, verified by `pnpm public:verify`).
- Node engine range allows `22.x` and `24.x` (preferred local version remains `24` via `.nvmrc`).
